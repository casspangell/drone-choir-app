
In the drone-choir-app directory, run:
### `npm start`

Open [http://localhost:8080](http://localhost:8080) to view it in your browser.

Open another terminal in the drone-choir-app directory and run:
### `node server.js`

Open another terminal in the drone-choir-app directory and run:

### `node api-server.js`

Run this command in the root folder to start the api server

### `python mycelial.py`

Run this command in the root folder of freesound-api-project